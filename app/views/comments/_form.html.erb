<%= form_with model: [ @post, @post.comments.build ] do |form| %>


    <%= hidden_field :commenter, value: current_user.name + " " + current_user.lastName %>

  <p>
    <%# form.label :body %>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>

